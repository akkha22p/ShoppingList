<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">#23A2B9</item>
        <item name="colorPrimaryDark">#B2E3EB</item>
        <item name="colorAccent">#066370</item>

    </style>
    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>
    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>
    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"/>

    <style name="scrollingActivityTheme" parent="AppTheme">
    <!-- Customize your theme here. -->
    <item name="fontFamily">@font/bigheart</item>
    </style>

    <style name="itemTheme" parent="AppTheme">
        <!-- Customize your theme here. -->
        <item name="fontFamily">@font/bigheart</item>
    </style>

</resources>
